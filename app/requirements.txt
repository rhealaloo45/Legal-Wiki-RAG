flask
gunicorn
pypdf
requests
python-dotenv
pymupdf
pytesseract
Pillow
openai
openpyxl==3.1.2
python-docx
# Phase 2 — PostgreSQL storage (only required when DATABASE_URL is set)
sqlalchemy>=2.0
psycopg2-binary
pgvector
# Phase 4.7 — Intent classifier agent (LangGraph orchestration)
langgraph>=1.0
langchain-core
# Opik — LLM tracing and evaluation (self-hosted)
opik
litellm==1.83.9
