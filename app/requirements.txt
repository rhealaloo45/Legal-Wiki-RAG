flask
gunicorn
pypdf
requests
python-dotenv
pymupdf
pytesseract
Pillow
openai
openpyxl==3.1.2
python-docx
# Phase 2 — PostgreSQL storage (only required when DATABASE_URL is set)
sqlalchemy>=2.0
psycopg2-binary
pgvector
# Phase 4.7 — Intent classifier agent (LangGraph orchestration)
langgraph>=1.0
langchain-core
# Offline RAG evaluation (Groundedness/Relevance/Retrieval against Azure OpenAI judge)
azure-ai-evaluation
azure-identity
cryptography>=42.0
# Phase 5 — Date/Deadline Agent. Public-holiday calendars for business-day
# offsets, resolved per document from its own Business Day clause or its
# recorded jurisdiction. Pure Python, no network call at runtime. Optional by
# design: services/calculation.py falls back to weekday-only counting and says
# so in the answer if the import fails.
holidays>=0.50
