# Open items

1. Precedent counts are approximate
2. Retrieval is keyword-only in the main chat
3. Citation checks verify against retrieved context, not the source document
4. Anomaly survey finds only value anomalies
5. Review/Compare tested once since the fixes
6. Golden 50 not re-run since the fixes
7. Party scan cap of 20
8. Two files tracked but matched by an ignore rule
